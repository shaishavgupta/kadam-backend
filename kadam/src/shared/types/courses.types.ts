import { ContentType } from '../enums';

export { ContentType };

// Note: All these interfaces are now defined as TypeBox schemas in ../schemas/course.ts
// and exported as Static types. Import them using:
// import {
//   Category, Module, Content, Course, Vector,
//   CreateCourseRequest, CreateModuleRequest, CreateContentRequest,
//   UpdateCourseRequest, ContentWithModule, PaginatedCoursesResponse
// } from '../schemas/course'

// PublishCourseRequest is now defined as a TypeBox schema in ../schemas/course.ts
// Import it using: import { PublishCourseRequest } from '../schemas/course'
